// // contracts/Box.sol
// // SPDX-License-Identifier: MIT
// pragma solidity ^0.8.0;

// import "@openzeppelin/contracts/access/Ownable.sol";

// contract Application is Ownable {
//   uint256 public appicationID;
//   address public applicant;
//   string public applicationData;
//   bool public isAccepted;



//   // Stores a new value in the contract
//   function store(uint256 newValue) public onlyOwner {
//     value = newValue;
//     emit ValueChanged(newValue);
//   }

//   // Reads the last stored value
//   function retrieve() public view returns (uint256) {
//     return { 'applicationID' : appicationID, 'applicantAddress': applicant, 'applicationData' : applicationData, 'isAccepted': isAccepted}
//   }
// }
